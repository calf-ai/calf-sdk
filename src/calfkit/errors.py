"""The error vocabulary: ErrorReport/ErrorDetail, ToolError (the retryable
exception tool authors raise), and the user-catchable exception family.
Bottom tier — messages.py imports ErrorDetail from here."""
