"""The per-kind processors (sketch §12): each kind's hop pipeline, composed
from the shared components. No module-level adapter imports — session/bus
factories are supplied by the composition root."""
